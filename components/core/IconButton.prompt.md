A circular, icon-only button; `label` is required.

```jsx
<IconButton label="Add to favourites" variant="outline"><HeartIcon /></IconButton>
```

Variants: ghost (toolbars), outline (on cards), solid (a single prominent action).
