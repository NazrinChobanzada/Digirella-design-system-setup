Category depth on product and listing pages.

```jsx
<Breadcrumb items={[{label:'Home',href:'/'},{label:'Electronics',href:'/e'},{label:'Headphones'}]} />
```

Never a substitute for a back link on a task flow — breadcrumbs describe hierarchy, not history.
