Switches between views in the same context. 2–6 tabs; beyond that, use a side menu.

```jsx
<Tabs items={[{value:'all',label:'All',count:24},{value:'new',label:'New'}]} value={t} onChange={setT} />
```
