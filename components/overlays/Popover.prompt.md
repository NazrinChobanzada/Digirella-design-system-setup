Rich anchored content — a shipping breakdown, a mini seller profile. For a single line of text use `Tooltip`; for a list of actions use `Menu`.

```jsx
<Popover open={o} onClose={close} trigger={<Button variant="ghost">Shipping</Button>}>…</Popover>
```
