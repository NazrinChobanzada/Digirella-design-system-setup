Mirror the real layout — same number of lines, same image ratio. Never a full-page spinner.

```jsx
<Skeleton shape="block" height={150} /><Skeleton width="70%" />
```

Requires the `digirella-shimmer` keyframes from `tokens/base.css`.
