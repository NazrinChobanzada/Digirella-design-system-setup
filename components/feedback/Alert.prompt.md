Inline banner that stays until the condition changes. Use `Toast` for transient confirmation and a field `error` for one input.

```jsx
<Alert tone="warning" title="Only 3 left in stock">Other buyers are viewing this item.</Alert>
```
